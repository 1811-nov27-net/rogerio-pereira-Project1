@model List<Project1.WebUi.Models.Customer>

@{
    ViewData["Title"] = "Customer";
}

<div class='text-center'>
    <h1>Customer</h1>

    <p>
        <a class="btn btn-success my-3" asp-area="" asp-controller="Customers" asp-action="Create">
            <i class="fas fa-plus mr-1"></i> Create New
        </a>
    </p>
</div>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb p-0 my-4">
        <li class="breadcrumb-item" aria-current="page">
            <a asp-controller="Home" asp-action="Index">Home</a>
        </li>
        <li class="breadcrumb-item" aria-current="page">
            Customer
        </li>
    </ol>
</nav>

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th width="150px">Actions</th>
            <th width="100px">Id</th>
            <th>First Name</th>
            <th>Last Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a class="btn btn-info btn-sm" asp-area="" asp-controller="Customers" asp-action="Edit" asp-route-id="@item.Id">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a class="btn btn-info btn-sm" asp-area="" asp-controller="Customers" asp-action="Details" asp-route-id="@item.Id">
                        <i class="fas fa-search"></i>
                    </a>
                    <a href="#" class="btn btn-danger btn-sm" data-href="/Customers/Delete/@item.Id" data-toggle="modal" data-target="#confirm-delete"
                       data-id="@item.Id" data-model="Customer" data-name="@item.FirstName @item.LastName">
                        <i class="far fa-trash-alt"></i>
                    </a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
            </tr>
        }
    </tbody>
</table>